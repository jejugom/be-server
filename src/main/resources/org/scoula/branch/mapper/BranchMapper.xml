<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.scoula.branch.mapper.BranchMapper">
    <select id="getAllBranches" resultType="org.scoula.branch.domain.BranchVO">
        SELECT code, name, tel, address, x, y FROM bank
    </select>

    <select id="getBranchByCode" resultType="org.scoula.branch.domain.BranchVO">
        SELECT code, name, tel, address, x, y FROM bank WHERE code = #{code}
    </select>

    <insert id="insertBranch">
        INSERT INTO bank (code, name, tel, address, x, y)
        VALUES (#{code}, #{name}, #{tel}, #{address}, #{x}, #{y})
    </insert>

    <update id="updateBranch">
        UPDATE bank
        SET name = #{name},
            tel = #{tel},
            address = #{address},
            x = #{x},
            y = #{y}
        WHERE code = #{code}
    </update>

    <delete id="deleteBranch">
        DELETE FROM bank WHERE code = #{code}
    </delete>
</mapper>
