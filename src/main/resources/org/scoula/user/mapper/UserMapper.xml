<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.scoula.user.mapper.UserMapper">
    <resultMap id="userMap" type="org.scoula.user.domain.UserVO">
        <id property="email" column="email"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="birth" column="birth"/>
    </resultMap>

    <select id="findByEmail" resultMap="userMap">
        SELECT email, name, phone, birth
        FROM user
        WHERE email = #{email}
    </select>

    <insert id="insertUser">
        INSERT INTO user (email, name, phone, birth)
        VALUES (#{email}, #{name}, #{phone}, #{birth})
    </insert>
</mapper>
