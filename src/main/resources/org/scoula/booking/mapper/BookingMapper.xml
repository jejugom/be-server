<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.scoula.booking.mapper.BookingMapper">
    <select id="getBookingsByEmail" resultType="org.scoula.booking.domain.BookingVO">
        SELECT id, email, bank_code, prod_code, date, time, doc_info
        FROM booking
        WHERE email = #{email}
    </select>

    <select id="getBookingById" resultType="org.scoula.booking.domain.BookingVO">
        SELECT id, email, bank_code, prod_code, date, time, doc_info
        FROM booking
        WHERE id = #{id}
    </select>

    <insert id="insertBooking" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO booking (email, bank_code, prod_code, date, time, doc_info)
        VALUES (#{email}, #{bankCode}, #{prodCode}, #{date}, #{time}, #{docInfo})
    </insert>

    <update id="updateBooking">
        UPDATE booking
        SET email = #{email},
            bank_code = #{bankCode},
            prod_code = #{prodCode},
            date = #{date},
            time = #{time},
            doc_info = #{docInfo}
        WHERE id = #{id}
    </update>

    <delete id="deleteBooking">
        DELETE FROM booking WHERE id = #{id}
    </delete>
</mapper>
